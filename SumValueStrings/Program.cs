using System;

namespace SumValueStrings
{
    public class Program
    {
        static void Main(string[] args)
        {
            Console.WriteLine("Hello World!");

            // if there is white space return result -1

            // switch statement to cycle thru word hello e=5, h=8, l=12, o=15
        }

        public static int ComputeStringValue(public string word)
        {
            int result = 0;
            //create a char list to store each letter in string as a single index value
            public char[] chars = word.ToCharArray(0,word.length);
            
        //string [] list;
            
            //first condition if the string has white space, return -1 --> result =-1
            if (string.IsNullOrWhiteSpace())
            {

            }

            return result;
        }
    }
}
